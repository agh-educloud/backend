version: '3'
services:
  mongo:
    image: 'mongo:latest'
    container_name: 'mongo'
    # TODO user/password
    # TODO init script
    ports:
      - '27100:27017'
  backend:
    build: .
    container_name: backend_container
    image: 'backend'
    command: go run main.go
    ports:
      - "3001:3001"

