syntax = "proto3";

import "user.proto";

service ChatService {
    rpc ExchangeMessages (stream ChatMessage) returns (stream ChatMessage);
}

message ChatMessage {
    User sender = 1;
    Message message = 2;
    string code = 3;
}

message Message {
    string content = 1;
    string timeStamp = 2;
}
